As for descriptions. Maybe click selects a building, double-click builds, click and drag builds, and there's a build button beneath description.
	Meanwhile, the selected building shows its description.

upgrades don't have description

deleting stairs should remove floor entries from map and stuff

building structures (class?) does not charge correctly (...when ...)
	I think maybe I meant that the building structure class isn't applying the way it should ...

scale animates for center col but not others  # None seem to scale now

New floors should fade in individually, rather than as a group